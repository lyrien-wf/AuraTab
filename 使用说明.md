# AuraTab 使用说明

> AuraTab 是一个 Chromium 浏览器扩展，接管浏览器的**新标签页**，把你的收藏夹（书签）渲染成一个个人主页：
> 左侧是收藏夹目录树，中间是时钟和搜索框，下方是当前目录的书签卡片。
>
> 适用浏览器：Edge 114+ / Chrome 114+ / 其他 Chromium 内核浏览器。

---

## 目录

1. [安装与加载](#1-安装与加载)
2. [主页使用](#2-主页使用)
3. [搜索](#3-搜索)
4. [设置页详解](#4-设置页详解)
5. [工具栏弹窗（Popup）](#5-工具栏弹窗popup)
6. [书签自动同步](#6-书签自动同步)
7. [开发者命令](#7-开发者命令)
8. [常见问题 FAQ](#8-常见问题-faq)
9. [隐私说明](#9-隐私说明)
10. [已知边界（v1）](#10-已知边界v1)

---

## 1. 安装与加载

### 1.1 从源码构建

需要 Node.js 18 及以上版本（推荐 20+）。在项目根目录执行：

```bash
npm install       # 安装依赖
npm run build     # 类型检查 + 构建，产物输出到 dist/
```

构建成功后 `dist/` 目录即为完整扩展。

### 1.2 加载到浏览器

**Edge：**

1. 地址栏打开 `edge://extensions`
2. 左下角开启「**开发人员模式**」
3. 点击「**加载解压缩的扩展**」，选择项目的 `dist/` 文件夹
4. 新开一个标签页，即可看到 AuraTab 主页

**Chrome：**

1. 地址栏打开 `chrome://extensions`
2. 右上角开启「**开发者模式**」
3. 点击「**加载已解压的扩展程序**」，选择项目的 `dist/` 文件夹
4. 新开一个标签页即可

> 💡 浏览器主页/启动页设置不受影响 —— AuraTab 只接管「新标签页」。若你安装过其他新标签页类扩展，后加载的会生效，建议在扩展管理页停用冲突的扩展。

### 1.3 更新与卸载

- **更新**：修改代码后重新 `npm run build`，然后到扩展管理页点击本扩展卡片上的「刷新 ⟳」按钮（或重新加载 `dist/`）。
- **卸载**：扩展管理页点击「移除」。你的书签数据不受任何影响（AuraTab 从不修改书签）。

---

## 2. 主页使用

### 2.1 页面布局

```text
┌──────────────┬──────────────────────────────────────────┐
│ AuraTab      │                 09:41                     │
│ ├ nas        │            星期三 3月12日                  │
│ │ ├ 本地     │      ┌──────────────────────────┐         │
│ │ └ hk       │      │ 🔍 搜索书签或网页          │         │
│ ├ AI         │      └──────────────────────────┘         │
│ ├ 邮件       │   ┌────┐ ┌────┐ ┌────┐ ┌────┐             │
│ └ ...        │   │卡片│ │卡片│ │卡片│ │卡片│             │
│ [⚙ 设置]     │   └────┘ └────┘ └────┘ └────┘             │
└──────────────┴──────────────────────────────────────────┘
```

> 搜索框左侧图标为当前**搜索引擎**（点击可切换，见 [3.3](#33-切换搜索引擎)）；页面**右上角**有「每行卡片数」快捷下拉（见 [2.4](#24-每行卡片数快捷调整)）。

### 2.2 左侧目录树

| 操作 | 方式 |
|---|---|
| 选中目录 | 单击目录名 → 右侧立即显示该目录的**直接书签** |
| 展开 / 折叠 | 单击目录前的 ▸ 箭头，或双击目录名 |
| 角标数字 | 目录名右侧的小数字 = 该目录下直接书签的数量 |
| 调整侧栏宽度 | 拖动侧栏右边缘（240–420px，自动记忆） |

**核心规则**：子文件夹只出现在左侧树里，右侧**永远只显示当前选中目录自己的书签**，不会把子目录的书签平铺上来，也不会渲染子文件夹卡片。选中一个没有直接书签、但有子目录的文件夹时，右侧会显示提示，并把子目录做成可点击的快捷跳转按钮。

**键盘操作**（焦点在树上时）：

| 按键 | 功能 |
|---|---|
| `↑` / `↓` | 上下移动 |
| `→` | 展开目录 / 进入第一个子目录 |
| `←` | 折叠目录 / 返回父目录 |
| `Enter` 或 `空格` | 选中当前目录 |
| `Home` / `End` | 跳到第一个 / 最后一个目录 |

### 2.3 书签卡片

卡片为居中布局：站点图标居中显示，下方是最多两行的标题（格子高度已按两行文字预留）。

| 操作 | 方式 |
|---|---|
| 打开（当前标签页） | 左键单击卡片 |
| 后台新标签页打开 | `Ctrl`（Mac 为 `Cmd`）+ 单击，或鼠标中键 |
| 右键菜单 | 「在新标签页打开」「复制链接」 |
| 查看完整信息 | 鼠标悬停卡片，原生 tooltip 显示完整标题 + URL |

- 卡片图标来自浏览器**本地缓存**的站点图标；没有缓存时自动降级为「域名首字母色块」（同一域名颜色恒定）。
- 标题过长自动截断为两行。
- 标记为「**已拦截**」的卡片：该书签使用了不安全协议（如 `javascript:`），点击无效 —— 这是安全防护，详见[隐私说明](#9-隐私说明)。

### 2.4 每行卡片数（快捷调整）

主页**右上角**有一个「每行卡片数」下拉（鼠标移上去会变清晰），可选 **自动 / 6 / 8 / 10**：

- **自动**：按窗口宽度自适应填充，卡片最小 148px
- **6 / 8 / 10**：固定每行显示的书签数量

切换后立即生效并保存到设置（与设置页「外观 → 每行卡片数」是同一项，双向同步）。

> 书签超过 300 条时网格自动启用虚拟滚动，固定列数下同样流畅。

### 2.5 时钟

搜索框上方显示当前时间与日期（如「星期三 3月12日」），可在[设置页](#4-设置页详解)配置 24 小时制、显示秒、显示日期。

### 2.6 窄窗口 / 小屏幕

窗口宽度小于 900px 时，侧栏自动收起为**抽屉**：点击左上角 ☰ 按钮唤出，点击遮罩、选择目录或按 `Esc` 收起。

### 2.7 状态记忆

下次打开新标签页时自动恢复：上次选中的目录、侧栏宽度、全部设置。若设置了「默认打开目录」则优先使用它。

---

## 3. 搜索

搜索框是**双职责**的 —— 既搜本地书签，也能跳搜索引擎：

### 3.1 本地搜索书签

直接输入关键词，右侧网格**实时**替换为搜索结果：

- 匹配书签**标题**与 **URL**，不区分大小写
- 排序规则：标题前缀命中 > 标题包含 > 域名包含 > URL 包含，同分保持收藏夹原始顺序
- 最多显示 200 条，结果上方显示「共 N 条结果 / 清空」
- 搜索范围（全部书签 / 仅当前目录）可在设置页配置

| 按键 | 功能 |
|---|---|
| `Enter`（有结果） | 在当前标签页打开第一条结果 |
| `Enter`（无结果） | 跳转搜索引擎搜这个词 |
| `Esc` | 清空搜索，回到目录视图 |
| `/` | 页面任意位置快速聚焦搜索框 |

### 3.2 直接搜网页

输入以 **`?`** 开头的内容（如 `?rust 教程`），搜索框进入引擎模式（边框变色并提示），按 `Enter` 直接跳转所选搜索引擎的结果页，不查本地书签。

### 3.3 切换搜索引擎

搜索框**左侧图标就是当前搜索引擎**（Bing / Google / 百度 / 自定义），**点击图标**弹出下拉菜单，选择后立即切换并自动保存 —— 无需进设置页。设置页「搜索 → 搜索引擎」与此处双向同步。

> 自定义引擎需包含 `%s` 占位符，例如 `https://duckduckgo.com/?q=%s`（在设置页填写）；缺占位符时回退 Bing。引擎图标为内置绘制，不产生任何外部请求。

---

## 4. 设置页详解

打开方式：主页左下角「⚙ 设置」按钮，或扩展管理页的「详细信息 → 扩展程序选项」。修改后点击底部「**保存**」，已打开的新标签页会**实时生效**，无需刷新。

| 分组 | 选项 | 说明 |
|---|---|---|
| **收藏夹根目录** | 勾选根文件夹 | 决定左侧树显示哪些根。Edge 通常是「收藏夹栏 / 其他收藏夹 / 移动收藏夹」，Chrome 是「书签栏 / 其他书签 / 移动设备书签」。id 动态读取，可多选；全部取消勾选则主页显示引导空态 |
| **默认打开目录** | 下拉选择 | 打开新标签页时默认选中的目录；「不指定」= 使用上次浏览的目录 |
| **目录树** | 显示空目录 | 默认关闭：递归下去一个书签都没有的目录不显示 |
| | 默认展开层级 | 打开页面时树自动展开到第几层（0 = 全部收起，默认 1） |
| **搜索** | 搜索范围 | 全部书签 / 仅当前选中目录 |
| | 搜索引擎 | Bing / Google / 百度 / 自定义（`%s` 占位符必填，缺失时回退 Bing）。也可在主页点击搜索框左侧引擎图标快速切换 |
| **外观** | 主题 | 跟随系统 / 浅色 / 深色 |
| | 每行卡片数 | 自动（按窗口宽度填充，卡片最小 148px）或固定 6 / 8 / 10 列；主页右上角有快捷下拉，与此项同步 |
| **时钟** | 24 小时制、显示秒、显示日期 | 三个独立开关 |
| **高级** | 允许打开 file: 书签 | 默认关闭（拦截）。`javascript:` 等伪协议**无论如何都拦截**，无开关 |
| 底部 | 恢复默认 | 一键回到初始设置（根目录恢复为默认勾选第一个） |

> 设置存储在 `chrome.storage.sync`：登录了浏览器账号（Edge/Google）时会随账号跨设备同步。

---

## 5. 工具栏弹窗（Popup）

点击浏览器工具栏的 AuraTab 图标：

- **快捷网页搜索**：输入关键词按 `Enter`，用设置页选定的搜索引擎在**新标签页**打开结果（也支持 `?` 前缀）
- **⚙ 设置**：直接打开设置页
- **主题**：一键在「跟随系统 → 浅色 → 深色」间循环切换，立即同步到设置

---

## 6. 书签自动同步

在浏览器任何位置增删改书签（收藏夹管理器、地址栏收藏按钮、HTML 批量导入……），AuraTab 都会**自动刷新**：

- 后台脚本监听全部 6 类书签变更事件
- 300ms 防抖（批量导入几千条也只触发一次重建）
- 已打开的新标签页热更新目录树和卡片，**当前选中的目录保持不变**（若该目录被删除，自动回退到默认目录或第一个根）

无需手动刷新页面。

---

## 7. 开发者命令

```bash
npm install         # 安装依赖
npm run dev         # 开发服务器 → http://localhost:5173/newtab.html
npm run build       # 类型检查 + 完整构建 → dist/（用于加载扩展）
npm run build:fast  # 跳过类型检查的快速构建
npm test            # 运行单元测试（Vitest，36 个用例）
npm run test:watch  # 测试监听模式
npm run icons       # 重新生成扩展图标（public/icons/*.png）
```

> `npm run dev` 在普通浏览器里预览时没有扩展 API，会自动降级为**内置演示书签数据** + 内存存储，方便纯前端调试；真实数据需按 [1.2](#12-加载到浏览器) 加载 `dist/` 后验证。

---

## 8. 常见问题 FAQ

**Q1：加载扩展后新标签页没变化？**
检查是否安装了其他「新标签页覆盖」类扩展（冲突时后者生效，请停用其一）；确认加载的是 `dist/` 目录而不是项目根目录；改动代码后记得重新 `npm run build` 并在扩展管理页点「刷新 ⟳」。

**Q2：主页是空的 / 提示「尚未选择收藏夹根目录」？**
打开设置页，在「收藏夹根目录」里勾选至少一个根文件夹并保存。也可能是勾选的根下所有目录都没有书签（空目录默认隐藏），可临时打开「显示空目录」确认。

**Q3：为什么有的卡片图标是彩色字母块？**
浏览器本地没有缓存该站点的图标（新加的书签、从未访问过的站点常见）。访问一次该网站后，图标会自动出现 —— AuraTab 只用浏览器本地 `_favicon` 缓存，不请求第三方图标服务。

**Q4：某个书签点了没反应，卡片右上角有「已拦截」？**
该书签的 URL 使用了 `javascript:`、`data:` 等不安全协议，出于 XSS 防护被强制拦截（这类书签通常来自导入的第三方收藏文件）。`file:` 本地文件书签默认也拦截，可在「设置 → 高级」中放开。

**Q5：搜索结果太少 / 想只搜当前目录？**
默认搜索范围是「全部书签」，结果上限 200 条（防止一次渲染过多）。想精确找某个目录下的书签，先在左侧选中目录，再到设置页把搜索范围改为「当前目录」。

**Q6：如何换搜索引擎为 DuckDuckGo 等？**
设置页 → 搜索 → 引擎选「自定义」，填入含 `%s` 的地址，如 `https://duckduckgo.com/?q=%s`。

**Q7：想彻底清掉 AuraTab 的本地数据？**
卸载扩展即清除全部（设置、目录快照、界面状态都存在浏览器为扩展分配的 `chrome.storage` 里，无其他残留）。

**Q8：目录树里看不到某个空文件夹？**
默认行为（`showEmptyFolders = false`）。在设置页打开「显示空目录」即可看到。

---

## 9. 隐私说明

- **不收集、不上传任何数据**。整个扩展没有任何网络请求代码（无 `fetch` / `XMLHttpRequest`）；唯一的"出网"是你主动按 `Enter` 跳转搜索引擎，那是普通的页面导航。
- 书签数据只在**本地内存**与浏览器扩展存储（`chrome.storage`）中流转；`storage.sync` 仅同步设置项（几 KB），由浏览器厂商账号体系托管。
- 权限最小化：仅 `bookmarks`（读书签）、`storage`（存设置/缓存）、`favicon`（读本地站点图标）；不申请 `tabs`、`history`、`<all_urls>`。
- 严格 CSP，不使用 `eval`、不注入远程脚本；书签标题一律作为文本渲染，防止恶意书签标题 XSS。
- 打开链接前校验协议，仅允许 `http / https / ftp`（`file:` 默认拦截、可配置），始终拦截 `javascript:` 等伪协议。

---

## 10. 已知边界（v1）

- **只读**：不能在主页里增删改书签；右键菜单仅「在新标签页打开」「复制链接」。写操作留待 v1.1 评估。
- 暂不支持拼音首字母搜索（计划 v1.1）。
- 无障碍已实现语义、焦点环、`aria-live` 播报、对比度等基础保障；完整 WCAG 合规需人工辅助技术实测。
